# 模式 B · 功能模块 PRD 写作指南

> **产出文件：** `.context/vibe-product-design/<功能名 slug>/PRD.md`（单文档，不拆章、不需要合并脚本）
> **输入：** 已有产品背景（若存在模式 A 的 `PRD.md`/分章文件，直接读取引用） + 轻量问卷澄清结论 +
> 高保真原型图（平台与风格由配置驱动，默认 PC 端 2K ＋ 苹果设计风格，存于同目录 `assets/`）。
> **作用：** 让团队里的每个角色，拿着**这一份文档**就能各自开工——不需要再追问你、不需要再开会对齐。
> **写作节奏（PRD ⇄ 原型图 的螺旋迭代）：** 先按 1→9 章写初稿把业务逻辑想清楚（第 5 章各页面小节
> 的原型图位置先留空）→ 从第 5 章的「页面 × 状态」反推图单 → 出图 → **图文互检**（攒「设计发现
> 清单」：漏了什么/想错什么/新灵感/图文不一致 → 逐条修订进对应章节 → 把图链接进各页面小节）→
> 修订若改变界面设计则补画/重画受影响的图、再对一遍 → 收敛后格式规范化。画图是一次设计推演，图出来
> 后 PRD 必须跟着变得更完整；文档和原型互为草稿、互相打磨，交付一对互相印证的 PRD ＋ 原型图——这是
> **设计输入版**。设计师据此完成正式设计稿后，用户带着定稿切图回来，走**设计稿回填**（阅读切图 →
> 攒差异清单 → 回填全文 → 第 5 章切换为定稿切图）形成**终版 PRD**——始终是同一份 `PRD.md`，
> AI 原型图留存进"存档"小节不删除。详细步骤见 `SKILL.md`「模式 B · 执行步骤」，自查方法见下方
> 「图文互检的自查清单」与「设计稿回填：从定稿切图到终版 PRD」。

## 章节顺序即叙事（先读懂这条线，再动笔）

九个章节是一条从"为什么"走到"怎么上线"的叙事线，每一章回答一个问题、并为下一章铺垫：

```text
1 背景与目标      为什么做、做成什么样算成功
2 用户与场景      为谁做——目标达成是通过满足谁的哪些场景（场景编号 S1…）
3 范围与约束      这次动多大——哪些页面/角色/系统受影响，边界和硬约束在哪
4 业务逻辑与流程   它怎么运转——流程、规则（R1…）、权限、异常（E1…），与界面无关的骨架
5 功能清单与UI/UE  它长什么样、要交付什么——对着原型图逐页讲功能点，验收标准就位
6 测试关注点      怎么验——第 4/5 章覆盖"应该怎样"，这里专列容易漏测的边界
7 数据与埋点      上线后怎么知道第 1 章的目标达成了没
8 风险与开放问题   还有什么没想清楚、可能翻车
9 发布计划        怎么安全地放出去
```

**环环相扣的三个写法，全文贯彻：**

- **编号引用，绝不重述。** 场景编 `S1…`（2.2）、业务规则编 `R1…`（4.2）、异常编 `E1…`（4.4）、
  功能点用 ID（第 5 章）。后面的章节引用编号即可——第 5 章功能点写"规则见 R3"，不把 R3 抄一遍；
  第 6 章测试写"覆盖 E2 的三种触发条件"，不重画异常表。
- **章首一句承接。** 每章开头用一句话接住上一章的结论（模板里的引导语给了示例语气），让读者顺着
  读不断线，而不是九个独立板块的拼盘。
- **前后呼应闭环。** 第 7 章埋点要能回答第 1 章的目标；第 6 章边界用例来自第 4 章的异常和第 5 章
  的验收标准；第 8 章开放问题收留所有前文标了 `[假设]`、`[待确认]` 的地方。写完通读一遍，检查这些
  线都连上了。

## 写给谁看，这份 PRD 就该让谁一眼看懂

这是本模板最重要的检验标准，写完自查一遍：

| 角色 | 该看哪几章 | 看完应该能回答的问题 |
| --- | --- | --- |
| UI/UX 设计师 | 第 2、5 章 | 每个页面承载哪些功能点和状态？交互怎么走？要复用哪些现有组件？ |
| 研发 | 第 4、5 章 | 业务规则是什么？状态怎么流转？异常怎么处理？每个页面要交付哪些功能点？ |
| 测试 | 第 5、6 章 | 每个功能点怎么算做对了（验收标准）？要测哪些边界值、异常输入、权限组合？ |
| 产品经理 | 全篇，重点第 1、5 章 | 目标达成没有？范围有没有跑偏？能不能验收？ |

如果某一章的读者看完还要来问你"那具体是什么意思"，这一章就没写到位。

## 写作要求

- **收口，不做全量分析。** 不要重新分析用户画像、竞品、商业模式——那是模式 A 的事。这里只服务
  **这一个功能点**：它解决什么问题、怎么设计、怎么运转、边界在哪。
- **像造火箭说明书一样精确。** "优化交互体验"是废话；"提交按钮在表单未填完时置灰，鼠标悬停提示
  缺失字段"才是内容。每条业务规则、每个异常分支都要具体到可以直接转成判断条件或测试用例。
- **对着图讲功能，不看图说话。** 第 5 章以页面为单位组织需求——这是评审的真实方式：对着原型图讲
  这个页面有哪些功能点、每个功能点的用户故事和要求是什么。图负责回答"在哪、长什么样"，文字负责
  回答"是什么、怎么算做对了"。**不要在 PRD 里描述配色、字号、圆角、间距这类视觉细节**——那些
  最终以设计稿为准；PRD 关注功能设计、用户故事、交互逻辑、验收标准。
- **只讲产品需求，不做技术方案设计。** 讲清楚业务规则、状态流转、异常时用户/系统的行为——不负责
  设计数据库表结构、接口协议、技术选型；时序图和状态机画的是"谁在什么条件下做了什么"，不是技术
  实现方案。这份文档要留给设计师、研发、测试各自去做专业判断，PM 负责把"要什么、为什么、边界在哪"
  定清楚。
- **必画异常路径。** 核心流程图和业务规则里，每一步都问"这一步失败/超时/被拒绝会怎样"——这是
  和模式 A 第 04 章一致的原则，此处同样适用，不能省。
- **文字要能脱离图独立成立。** 图是辅助，设计师和研发都可能看不到图（渲染失败/离线阅读）：每个
  功能点的需求描述与交互说明，必须让没看到图的人也能理解逻辑；图下只标页面名与状态，不承载逻辑。
- **可追溯、不臆造背景。** 涉及现有产品的部分（已有页面、已有组件、已有数据）要基于用户提供的真实
  信息或已有 PRD 文件；不确定就问用户，不要编。外部事实（技术基准、合规要求等）仍遵循 SKILL.md
  的「联网核实」原则。
- **优先级/工作量/编码复用现成规范。** 见 `输出格式规范.md` 的 P0–P3、XXS–XXL、Low–Very High
  分级，不要重新发明一套。

## 输出结构

```markdown
# 功能模块 PRD · <功能名称>

## 文档信息
- **关联产品/模块：** <这个功能挂在哪个现有产品/模块下；若有模式 A 全量 PRD，写"详见 ../../PRD.md">
- **状态：** 草稿 / 待评审 / 已评审 / 已发布
- **关联链接：** <现有全量 PRD、设计规范、相关工单等路径或链接>

## 变更记录
| 日期 | 版本 | 维护人 | 关键改动 |
|---|---|---|---|
| 2026-07-01 | v1.0 | 程知微 <zhiwei.cheng@example.com> ＋ Claude (vibe-product-design) | 初始版本 |

## 1. 背景与目标
### 1.1 需求背景
为什么现在要做这个功能？谁提出的？有什么数据或反馈支撑？（引用真实信息，没有就问用户）

### 1.2 目标
做成什么样算成功？尽量可衡量（数字/行为指标），不要写"提升体验"这种空话。第 7 章的埋点要能
回答这里的每一条目标。

### 1.3 非目标 / 明确不做
这一版**刻意不做**什么，避免范围蔓延、避免评审时被追问"那 XX 呢"。

## 2. 用户与场景
（章首承接示例："上面的目标，是通过满足以下用户在以下场景中的诉求来达成的。"）

### 2.1 目标用户
这个功能服务谁？（若模式 A 已有用户画像，写"见 ../../PRD.md 第01章"+一句提要；没有就简述）

### 2.2 典型使用场景
按最常见到最边缘列 2–5 个场景，编号 `S1…` 供第 4/5 章引用。每个场景一两句话说清楚
"谁、在什么情况下、想干什么、期待得到什么"：
- **S1（最高频）：** <谁> 在 <什么情况下> 想 <干什么>，期待 <得到什么>
- **S2：** …

## 3. 范围与约束
（章首承接示例："为满足上述场景，这次改动会触及以下范围，并受以下条件约束。"）

### 3.1 影响范围
涉及哪些**现有**页面、模块、角色、系统？新增了什么、改动了什么、删除了什么，列清楚。

### 3.2 前置依赖
依赖哪些现有能力/数据/第三方服务？依赖不满足会怎样？

### 3.3 假设与约束
做这个设计时假定为真、但未必验证过的前提，标 `[假设]`；已知的硬约束（性能、合规、技术栈）列出来。
标了 `[假设]`/`[待确认]` 的条目要在第 8 章开放问题里有着落。

## 4. 业务逻辑与流程
（章首承接示例："在上述范围内，这个功能这样运转——先看整体流程，再看每条规则与异常。"）
> 这一章是功能与界面无关的**运转骨架**，给研发看：看完应该理解业务规则、状态流转和异常处理该怎么
> 表现，不用再来问产品的意图；具体怎么实现（数据结构、接口、技术选型）由研发自己判断。第 5 章的
> 每个页面都挂在这里的某个流程节点上。

### 4.1 核心流程
用 Mermaid 画，画法和示例见 `输出格式规范.md`「图表（Mermaid）」一节：涉及多角色/多系统协作用
「业务流程图」的时序图写法（`sequenceDiagram`，**必含 `alt/else` 异常分支**）；如果这个功能只是
单一角色在系统内部的状态流转，改用「功能架构图」的 `flowchart` 写法即可，同样要画出异常/失败分支，
不要只画快乐路径。图后配几句文字：每个关键步骤"用户做什么 → 系统表现什么"，并标注对应场景（S1…）。

### 4.2 业务规则详解
把每条规则、计算逻辑、状态机讲清楚，编号 `R1…`，具体到可以直接转成判断条件或测试用例：
- **R1：** <触发条件> → <处理逻辑> → <结果>
- 状态机（如适用）：状态清单 + 每个状态可以流转到哪些状态 + 触发流转的事件

### 4.3 权限与角色矩阵
| 角色 | 可见 | 可操作 | 说明 |
|---|---|---|---|
| … | … | … | … |

### 4.4 异常与边界处理
每个失败分支怎么处理（重试 / 回滚 / 提示文案 / 降级），编号 `E1…` 逐条列出，不要笼统写"异常提示"：
| 编号 | 异常场景 | 触发条件 | 系统行为 | 用户看到什么 |
|---|---|---|---|---|
| E1 | … | … | … | … |

## 5. 功能清单与 UI/UE
（章首承接示例："上面的流程与规则，最终通过以下页面呈现给用户——下面对着原型图逐页讲功能。"）
> 这一章是**评审的主战场**，也是设计师和研发的开工依据：像评审会上对着原型图讲需求一样，每个页面
> 一小节——先放图，再讲这个页面上的功能点。图回答"在哪"，功能点回答"是什么、怎么算做对了"；
> 不要描述配色、字号等视觉细节，那些以设计稿为准。

### 5.1 功能总览
一张索引表看全貌，详情在后面各页面小节展开，此处不重复：

| ID | 功能点 | 所在位置 | 优先级 | 工作量 |
|---|---|---|---|---|
| ANC-01 | <功能点名> | 5.2 <页面名> | P0 | S |
| ANC-05 | <功能点名> | 5.x 页面之外 | P1 | XS |

编码规则（不套模式 A 的 L1–L4 四级体系——模式 B 是单个功能点，套四级反而繁琐）：
- 若这个功能挂在某个已有模式 A 全量 PRD 之下、且那份 PRD 里已经给这块业务定过 L1 模块码（如
  `PAY`），**复用那个 L1 码**接两位流水号，如 `PAY-01`、`PAY-02`……
- 若没有可复用的 L1 码，从功能名提炼一个 2–3 位大写字母前缀（如"降噪模式切换"→`ANC`），接两位
  流水号，如 `ANC-01`。
- 每个 ID 对应一条可独立验收的用户故事，不再往下拆——这份 PRD 本身就是最小颗粒度。

### 5.2 页面 · <页面/界面名称>
**流程位置：** 对应 4.1 的哪个节点、用户从哪进来（引用，不重述）。

![图 5-2a · <页面名>（默认态）](assets/<文件名>.png)

![图 5-2b · <页面名>（空状态）](assets/<文件名>.png)

（同一页面的不同状态各一张图，图题只标"页面名＋状态"，不写视觉描述；设计输入版引用 `assets/`，
设计稿回填后切换为 `design/`。）

**本页功能点：**

#### ANC-01 <功能点名>
- **用户故事：** 作为 <谁>，我想要 <干什么>，以便于 <得到什么价值>。（对应场景 S1）
- **需求描述：** 这个功能点的具体要求——要素、文案要点、默认值、开关条件；涉及的业务规则引用
  编号（如"计算逻辑见 R2"），不重抄。
- **交互说明：** 用户操作 → 界面/系统如何响应；状态切换对应上面哪张状态图；失败/边界时的表现
  引用异常编号（如"网络失败走 E1"）。
- **验收标准：** Given <前提> When <操作> Then <预期结果>（可多条；测试直接转用例，PM 逐条打钩）

（每个功能点重复上面的结构；每个页面重复 5.2 的小节结构，依次编 5.3、5.4……）

### 5.x 页面之外的功能点
原型图无法展现、但属于本次交付的功能——后台任务、推送/通知、定时清理、配置开关等。每条结构与
页面内功能点一致（ID / 用户故事 / 需求描述 / 验收标准），一样进 5.1 总览表。没有就写"无"。

### 5.y 与现有设计规范的一致性
复用哪些现有组件/交互模式（列出处）？哪些是本功能新增、需要设计师新出的？

### 5.z 早期 AI 原型（存档）
（仅在设计稿回填后存在：以下为设计定稿前的 AI 原型图，仅作过程存档，一切以上方设计定稿为准。
原 `assets/` 目录与图片保留不删。）

## 6. 测试关注点与边界用例
（章首承接示例："第 4 章的异常与第 5 章的验收标准已覆盖'应该怎样'，这里专列容易漏测的边界。"）
> 这一章是给 **测试**看的：看完应该能直接写测试用例，不用再来确认边界在哪。

按类型分组，每条给出"输入/条件 → 预期结果"，可引用异常编号（E1…）与功能点 ID：
- **边界值：** 空值、最大/最小值、临界数量、极限并发数……
- **异常输入：** 非法格式、超长文本、恶意输入……
- **权限相关：** 越权访问、角色切换、登出重登……（对照 4.3 矩阵逐格检查）
- **并发/时序：** 同时操作、网络延迟、重复提交……
- **兼容性：** 不同终端/版本/网络环境下的表现（如适用）……

## 7. 数据与埋点
（可选，仅在涉及时写。章首承接示例："上线后用以下数据回答第 1 章的目标是否达成。"）
从产品视角出发：这个功能上线后想回答什么业务问题（逐条对应 1.2 的目标）、要观察用户哪些关键行为
（如点击、完成率、放弃节点），需要埋哪些点——具体的数据字段/表结构设计交给研发，这里不展开。

## 8. 风险与开放问题
前文所有标 `[假设]`、`[待确认]`、评审中未拍板的事项，都要在这里有一条着落：
| 风险/问题 | 影响 | 应对/待确认 |
|---|---|---|
| … | … | … |

## 9. 发布计划
（可选）灰度策略、发布顺序、回滚方案、上线检查清单。
```

> 第 5 章的小节编号说明：5.1 固定是功能总览；从 5.2 起每个页面一节；页面讲完后顺延编号依次是
> 「页面之外的功能点」「与现有设计规范的一致性」；「早期 AI 原型（存档）」仅在设计稿回填后追加为
> 第 5 章最后一小节。模板里的 5.x/5.y/5.z 是占位写法，落笔时用实际顺延的编号。

## 原型图生成规范（config 驱动平台与风格 · 用 `zenmux-image-generation` 出高保真界面稿）

产出的是**高保真界面稿**，把每个关键界面最重要的设计决策展现出来——不是灰框线框图。

**平台与风格都不写死在这份文档里，而是由配置驱动**。用户只填两个字段：

- **`platform`（目标平台，强约束）**：只选一个（默认 `web`，即 PC 端 2K）。本功能**所有页面级
  原型图**的分辨率（`size`）、画幅与界面形态（布局范式、导航结构、信息密度）一律按这个平台出——
  配置了 PC 端，就全部出 PC 端的图，不要有的图画成手机端；仅组件/局部特写这类非页面级配图可临时
  用其它尺寸（如 1024x1024 方形），风格仍须一致。
- **`active_theme`（风格主题）**：一句话（默认苹果设计风格）。skill 通过一步**预处理**把这句主题
  用大模型展开成一份可复用的「风格提示词文件」并缓存，出图时**加载**这段风格提示词、拼在每张图的
  具体画面描述之前。用户不需要手填配色/字体/留白等一堆细节。

**配置存放在用户项目的 `.context` 下，不在 skill 目录里**——`style_prompt.ts` 首次运行会把 skill
的配置模板复制到 `<project-dir>/.context/vibe-product-design/config/`（只在缺失时复制，不覆盖用户
改动），之后读写都在这里，这样 skill 升级覆盖自己的文件时不会清掉用户配置与缓存。

```text
prototype-style.yaml（.context 下）：platform（目标平台）＋ active_theme（一句话风格主题）
  → 预处理：style_prompt.ts plan → 若无缓存，按 brief 用大模型展开成 .context/.../generated/<slug>.style.md
  → 加载：style_prompt.ts load → 取出风格提示词前缀 ＋ rendering（model/quality/size，size 已按 platform 定死）
  → 每张图 prompt = 风格提示词前缀 + 本图具体画面 → 交给 zenmux-image-generation 出图
```

`<skill-dir>` = 含本 skill `SKILL.md` 的目录（`功能模块PRD.md` 在其 `references/` 下，即上一级）。
脚本是 TypeScript，用 `npx --yes tsx <脚本>` 运行（本机需有 Node.js/npm，`npx` 会按需拉起 tsx/prettier）。

**先写初稿，再定图单。** 在动手出图之前，先把 PRD 初稿写完（第 1–4 章、第 5 章的文字部分——功能
总览与各页面小节的功能点，原型图位置先留空——以及第 6–9 章）——初稿的第 4 章已把状态、流程分支、
异常想清楚，第 5 章已按页面组织好功能点，**图单就是第 5 章的「页面 × 关键状态」清单**（覆盖主流程
关键节点 + 关键异常/空状态），一次性把所有画面描述列出来让用户确认，再统一生成，减少来回调用次数。
哪张图该画什么、该突出什么状态，都应该能从初稿内容里直接推导出来，不是凌空另起一套画面清单；
初稿里没讲清楚、也推不出来的细节，先照 `澄清问卷.md` 的方法问用户。

### 第 1 步 · 加载平台与风格（预处理 + 缓存）

先跑 `plan`（带 `--project-dir`，指向用户项目根）。首次运行它会把 skill 的配置模板复制到
`<project-dir>/.context/vibe-product-design/config/`，再解析目标平台与当前主题、检查缓存，告诉你
风格提示词是否已就绪：

```bash
npx --yes tsx <skill-dir>/scripts/style_prompt.ts plan --project-dir <project-dir>
```

看返回 JSON 的 `status`：

- **`status: ready`** → 已有缓存的风格提示词（默认的 `apple` 开箱即带，直接就绪）。
  `prompt_prefix` 就是要拼在每张图前面的风格前缀，`rendering` 是出图参数（其中 `size` 已按配置的
  `platform` 定死）。直接进第 2 步。
- **`status: need_generation`** → 该主题还没展开过（用户换了新主题）。返回里的 `brief` 是一份
  **展开任务书**：按它把这句主题**用大模型展开成一段风格提示词**（配色/字体/留白/组件/氛围/规避，
  并内化通用质量底线），写入 `brief` 指定的 `style_file`（即 `.context/.../config/generated/<slug>.style.md`）。
  写完这一步即完成预处理——之后同一主题会一直命中缓存，不必重复展开。写完再跑一次 `load` 取回：

  ```bash
  npx --yes tsx <skill-dir>/scripts/style_prompt.ts load --project-dir <project-dir>
  ```

> 换风格：让用户改 `.context/vibe-product-design/config/prototype-style.yaml` 的 `active_theme`
> ——写成 `themes:` 里的某个 key，或直接写一句自由主题文本（如"赛博朋克霓虹，暗色高对比"）。改完重跑
> `plan` 即按新主题展开。想强制重新展开（改了主题描述但 slug 没变时）加 `--force`。
> 换端：改同一文件的 `platform`（`platforms:` 里的某个 key，如 `web` / `mobile` / `tablet`）——
> 这是项目级设置，改完对后续所有页面级原型图生效；不要在单次产出里私自切换平台。改
> `<skill-dir>/config/` 里的模板没用——运行时以 `.context` 下的副本为准。零第三方依赖：脚本内置极简
> YAML 解析器，`npx` 会按需拉起 tsx，开箱即用。

### 第 2 步 · 逐图拼 prompt 并出图

**每张图的 prompt = 第 1 步拿到的 `prompt_prefix`（风格提示词）＋ 本图的具体画面描述。**
风格由前缀统一保证；画幅/设备**统一按配置的 `platform` 写**（`rendering.platform_label` 就是它的
可读名，如"PC / 桌面 Web（横版 2K）"），不要逐图另选设备。你只需为每张图写清楚**具体画面**：

> <画幅/设备：按配置平台写，如"桌面 Web 界面"（platform: web）或"竖版手机界面"（platform: mobile）>。
> 画面：<具体页面/状态名、关键区域与控件，
> 以及**此刻要突出的关键设计与交互状态**——例如"耳机 App 的降噪设置页：顶部返回箭头与标题
> '降噪模式'，主体是三张竖向排列的选择卡片'关闭 / 通透 / 强降噪'，当前选中'强降噪'卡片用细边框
> 加一枚小圆点标示选中态，卡片下方一行小字标注对应续航影响，页面底部留大量余白">

关键：画面描述里一定要写清**此刻处于哪个状态**、**要突出哪个关键设计**（选中态 / 空状态 / 错误
提示 / 加载中 / 首次引导……），这样每张图才是在"展现设计决策"，而不是画一个通用空界面。同一功能
不同状态各画一张，用状态差异体现设计，不要把所有状态挤进一张图。（这几条通用质量底线已经写进风格
提示词前缀，此处再强调一次，撰写画面描述时对齐。）画面描述是给**出图模型**看的，可以写视觉细节；
但这些细节不要回流进 PRD 正文——PRD 里只留功能与交互逻辑。

**出图参数**用第 1 步 `plan`/`load` 返回里的 `rendering`（来自 `config/prototype-style.yaml`，
所有主题通用，可在配置里改）：`model`（默认 `openai/gpt-image-2`，文字渲染稳、支持自定义尺寸，别用
fast/low 档以免糊字错字）、`quality`（默认 `high`，密集中文必须 high）、`size`（**已由配置的
`platform` 定死**，如 `web` = 2560x1440（PC 2K）、`mobile` = 1024x1536、`tablet` = 2048x1536）。
**`size` 是强约束**：所有页面级原型图一律用它，不要按图自选尺寸；仅组件/局部特写这类非页面级配图
可临时用其它尺寸（如 1024x1024 方形），风格与平台语境仍须一致。具体调用方式、参数含义以
`zenmux-image-generation` 自己的 `SKILL.md` 为准。

### 第 3 步 · 输出路径与数量

生成时**显式指定输出目录**到当前功能的 `assets/` 子目录，不要用 `zenmux-image-generation` 自己的
默认输出路径，否则 PRD 里的相对路径引用会失效：

```bash
--output-dir "<project-dir>/.context/vibe-product-design/<功能 slug>/assets"
```

一个功能通常配 3–8 张图（覆盖主流程每个关键节点 + 1–2 个关键异常/空状态），不凑数、不漏核心
"啊哈"节点或异常提示状态。**每个界面/状态生成 1 张即可**（关键主界面拿不准方向时可生成 2 张对比），
不需要该 skill 默认的 4 变体。生成完成后，在第 5 章对应页面小节用相对路径 `assets/<文件名>.png`
引用（PRD.md 与 `assets/` 同级，相对路径天然生效，换机器/换用户也不会失效），图题只标
"图 N · 页面名（状态）"。

### 前置 · 检查/安装 `zenmux-image-generation`

上面第 2 步出图依赖 `zenmux-image-generation` skill。先检查本机是否已装（常见安装路径如下，装法
不同路径也会不同，找不到就直接跳到安装命令）：

```bash
ls ~/.agents/skills/zenmux-image-generation 2>/dev/null || \
ls <project-dir>/.agents/skills/zenmux-image-generation 2>/dev/null
```

未安装则用 `npx skills add` 安装（`npx` 需要本机已有 Node.js/npm 环境）：

```bash
# 项目级安装（默认，只在当前项目可用）
npx skills add https://github.com/zenmux/skills --skill zenmux-image-generation

# 全局安装（-g，所有项目都能用；只需装一次，之后新项目也不用重复装）
npx skills add https://github.com/zenmux/skills --skill zenmux-image-generation -g
```

装完再跑一遍上面的检查命令确认路径存在即可使用；后续 `zenmux-image-generation` 自身的用法、模型
选择、参数说明，以该 skill 自己的 `SKILL.md` 为准，这里不重复。

## 图文互检的自查清单（出图后必做，这一步产出「设计发现清单」）

画原型图不是给 PRD 配插图，而是一次**设计推演**：把界面具体化时，你被迫替每个元素做出决策——放哪、
怎么写文案、选中态长什么样、出错了显示什么。这些决策里藏着初稿的漏洞和新的灵感。出图后（以及补画后
每一轮），**逐张图对照 PRD 初稿过一遍下面四类问题**，把答案攒成一份「设计发现清单」，再逐条修订进
文档——不要看一条改一条，先攒清单是为了看清发现之间的关联、并把拿不准的一次性问清楚：

| 类型 | 自查问题（逐张图问） | 典型例子 | 修订去处 |
| --- | --- | --- | --- |
| **漏了** | 画这张图时是否出现了初稿没写的状态/元素/分支？空状态、加载中、失败提示、首次引导画出来了吗——初稿有对应描述吗？ | 图里画了"列表为空"的引导文案，但 4.4 异常表里没有这一条 | 4.1 核心流程、4.2 业务规则、4.4 异常处理、第 5 章对应页面的功能点、第 6 章测试关注点 |
| **想错了** | 按初稿画出来后，流程/布局是否明显别扭？步骤是否多余、信息层级是否和业务优先级打架、两条规则是否在图上撞车？ | 初稿写"先选模式再看续航影响"，画出来发现续航信息藏太深，应直接标在选项卡上 | 4.1 核心流程、4.2 业务规则，以及第 5 章对应功能点的需求描述与验收标准 |
| **新灵感** | 具体化过程中是否冒出更好的交互/更简的流程/值得加的小设计？——**是否纳入本期是产品决策，列出来让用户拍板，不要擅自加范围** | 画选中态时想到"切换模式时给一句续航变化的即时反馈"，初稿没有 | 用户拍板后：纳入 → 相关各章＋图单；不纳入 → 第 8 章开放问题或 1.3 非目标 |
| **不一致** | 图和文字对不上的地方，以谁为准？文案措辞、按钮名称、状态数量、流程顺序，图与第 4/5 章是否完全一致？ | 图上按钮叫"开启降噪"，R2 规则里写的是"启用降噪模式" | 以正确的一方为准，统一改齐（正文、功能点交互说明；若图错了且影响理解，列入补画清单） |

清单攒好后：属于产品决策拿不准的（尤其新灵感取舍）先用 `AskUserQuestion` 问用户；然后逐条修订进
上表"修订去处"指向的章节，修订后通读一遍确保前后章节不矛盾。**若修订反过来改变了界面设计**（状态
增删、交互作废、流程节点变化），更新图单、只补画/重画受影响的那几张（风格前缀与出图参数不变），
再回到本清单对一遍。通常一轮回填＋至多一轮补画就该收敛；第二轮后仍大量冒新发现，说明初稿阶段没想
透，停下来和用户重新对齐范围。

## 完成后（设计输入版交付）

图文互检收敛（发现清单已全部修订进文档、图已链接进第 5 章各页面小节、补画的图也已对检）后，确认
每张图的图题与正文一致、功能点的交互说明与图相符，写入 `<功能 slug>/PRD.md`，运行：

```bash
npx --yes tsx <skill-dir>/scripts/format_feature_prd.ts <project-dir>/.context/vibe-product-design/<功能 slug>/PRD.md
```

规范化格式后，告诉用户文档路径、本次版本号、关键改动，以及原型图数量与存放路径。这份 `PRD.md`
即**设计输入版**交付物——同时提醒用户：拿它和 AI 原型图交给设计师出正式设计稿；设计稿完成后带着
切图回来，走下方「设计稿回填」形成终版 PRD。本次交付到此完成，不要等待设计稿。

## 设计稿回填：从定稿切图到终版 PRD

AI 原型图的使命是把设计意图讲清楚、给设计师一个高质量起点；**设计师的定稿才是产品最终长的样子**。
定稿相对原型几乎必然有出入——视觉重排、文案打磨、控件替换、新增的状态与细节、被砍掉的元素——其中
一部分只是视觉层面的，另一部分**实质上改变了业务规则或边界**。设计稿回填就是把这些出入系统性地
找出来、和用户确认、回填进 PRD，让 `PRD.md` 与设计定稿完全一致，成为下游 Agent（研发/测试/其它
自动化）唯一需要读的**终版设计文档**。

这个环节通常发生在设计输入版交付之后的**另一次会话**：用户说"设计稿好了""切图在 xx 目录""按设计稿
更新 PRD"之类，即进入本环节。前提是该功能文件夹下已有 `PRD.md`；没有就先确认用户是不是指别的功能。

### 第 1 步 · 用户指定切图位置，收进 `design/`

- 问清切图存放位置（本地目录或具体文件），`ls` 验证确有图片；空目录/找不到就回头核对，不要猜。
- `mkdir -p <功能 slug>/design`，把切图**复制**（不要移动用户原始文件）进去，文件名整理成
  `<序号>-<界面/状态名>.png` 的可读形式（如 `01-降噪设置页-默认态.png`）。命名对不上界面时先列
  文件清单让用户对号入座，再复制改名。
- 切图目录里混有与本功能无关的图（设计师常整包导出）时，列清单让用户勾选哪些属于本功能。

### 第 2 步 · 逐张阅读切图，攒「设计定稿差异清单」

用 `Read` 工具逐张读取 `design/` 下的切图（图片可直接阅读），**每张图对照现有 PRD 过一遍下面
四类问题**——方法与「图文互检的自查清单」同源，但对照对象从"初稿 vs AI 原型"换成了"现有 PRD vs
设计定稿"（重点对照第 4 章业务逻辑与第 5 章各页面小节的功能点与验收标准）：

| 类型 | 自查问题（逐张切图问） | 典型例子 | 回填去处 |
| --- | --- | --- | --- |
| **新增** | 定稿里出现了 PRD 没写的状态/元素/文案/交互暗示吗？设计师补的空状态、引导、动效示意，PRD 有对应描述吗？ | 定稿在切换降噪模式时加了一条续航变化的即时提示，PRD 没有 | 4.1 核心流程、4.2 业务规则、第 5 章对应页面（新增功能点条目或补交互说明，同步 5.1 总览表） |
| **改动** | 布局、流程顺序、控件形态、按钮/字段措辞与 PRD 一致吗？信息层级变了吗？ | PRD 写三张竖排卡片，定稿改成了分段控件（segmented control） | 第 5 章对应页面小节按定稿重写交互说明；措辞统一到 4.2 规则与各功能点验收标准 |
| **删减** | PRD 写了、定稿里没有的东西——是被设计师砍了，还是切图没覆盖到？ | PRD 写了"恢复默认"按钮，所有切图里都找不到 | 先问用户是砍了还是漏切；砍了→相关章节移除并记入 1.3 非目标或第 8 章 |
| **影响业务规则** | 上面的差异里，哪些不只是视觉，而是**行为/边界变了**？状态数量、流转触发条件、异常提示时机变了吗？ | 定稿把"确认弹窗"改成了"可撤销的 toast"，撤销窗口期是新的业务规则 | 4.2 业务规则、4.4 异常处理、第 5 章对应功能点的验收标准、第 6 章测试关注点 |

- 逐张看完先**攒成完整清单**再动笔改——和图文互检一样，先看全貌才能发现差异之间的关联。
- 清单里属于产品决策拿不准的，用 `AskUserQuestion` 让用户拍板——尤其两类：**定稿与 PRD 冲突以谁
  为准**（多数时候以定稿为准，但也可能是设计师理解偏了，不要默认任何一方永远正确）；**设计师新增
  的交互是否纳入本期范围**（纳入→回填各章；不纳入→记入第 8 章开放问题）。
- 切图看不清、对应哪个状态拿不准，问用户，不要看图编故事。

### 第 3 步 · 回填全文，切换第 5 章到定稿切图

- 把确认后的差异逐条回填到上表"回填去处"指向的**所有**章节，不是只改第 5 章。
- **第 5 章各页面小节的图切换为以 `design/` 切图为准**：相对路径 `design/<文件名>.png` 引用，
  图题沿用"图 N · 页面名（状态）"的写法；功能点的需求描述、交互说明、验收标准按定稿更新。原 AI
  原型图的引用整体挪进第 5 章末尾的「早期 AI 原型（存档）」小节，加一句"仅作过程存档，以设计定稿
  为准"。**`assets/` 目录与图片一律保留**——中间产物留存，不删。
- 「与现有设计规范的一致性」小节按定稿更新：定稿实际复用/新增了哪些组件与模式。
- 回填后**通读全文**：不允许任何章节还在描述已被定稿推翻的旧方案；正文、验收标准里的措辞
  （按钮名、状态名）与定稿完全一致。

### 第 4 步 · 版本、状态与规范化

- 变更记录追加一行：版本 +1，关键改动写明"设计稿回填：以定稿切图（N 张）更新第 5 章及相关业务
  规则/验收标准，形成终版"。
- 文档信息的**状态**改为"已评审"（或用户认可的终版表述）。
- 再跑一次 `format_feature_prd.ts` 规范化。
- 完成后告诉用户：终版 PRD 路径、版本号、回填的切图数量、采纳了哪些关键差异、哪些进了开放问题。
  此后下游 Agent 直接读这份 `PRD.md`，即可获得与设计定稿一致的完整设计。
