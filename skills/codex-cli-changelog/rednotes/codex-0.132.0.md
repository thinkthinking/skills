Codex 0.132.0 这版主要把 Python SDK 的登录、账号检查、logout 和 turn 结果处理补成了一套更完整的开发体验，也让 `codex exec resume` 可以在保留 session context 的同时继续约束 structured JSON output。

如果你在用 Codex 做自动化、多轮 agent workflow 或远程执行，这次更新值得看：TUI 启动更快，remote session 更稳，图片输入的 fidelity 也能更好地保留下来。

完整原版 ChangeLog 见图2（中文版）、图3（英文原版）。
